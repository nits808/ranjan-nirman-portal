# API base URL for production builds. Omit or use /api when the SPA is served
# behind the same origin as the backend (e.g. nginx proxies /api to Spring).
# Example: VITE_API_BASE=https://api.yourdomain.com/api
VITE_API_BASE=
